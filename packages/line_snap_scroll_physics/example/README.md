# line_snap_scroll_physics example

A minimal demo showing `LineSnapScrollPhysics` and
`LineSnapScrollController` applied to a `ListView` of fixed-height rows.
Scroll offsets quantize to line boundaries — fling, drag, and
programmatic scroll always land on an exact row.
