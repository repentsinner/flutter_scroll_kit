# repl_view example

A minimal demo wiring a handful of fake `ConsoleEntry` items into
`ReplView`. Input entries pin as sticky headers; response entries scroll
as leaves within their surrounding input's scope.
