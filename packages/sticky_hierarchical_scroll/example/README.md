# sticky_hierarchical_scroll example

A minimal demo showing `StickyHierarchicalScrollView` rendering a nested
list of sections and leaf items. Section headers stack at the viewport
top as their scope scrolls past, VS Code–style.
