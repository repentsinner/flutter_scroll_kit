name: sticky_hierarchical_scroll
description: >-
  VSCode-inspired sticky hierarchical scroll view for Flutter. Displays
  hierarchical data with fixed-height rows and sticky section headers.
version: 0.1.0
homepage: https://github.com/repentsinner/flutter_scroll_kit
repository: https://github.com/repentsinner/flutter_scroll_kit/tree/main/packages/sticky_hierarchical_scroll
issue_tracker: https://github.com/repentsinner/flutter_scroll_kit/issues
topics:
  - scroll
  - sticky-header
  - hierarchy
  - flutter

resolution: workspace

environment:
  sdk: ^3.10.0

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
