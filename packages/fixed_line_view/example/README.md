# fixed_line_view example

A minimal demo showing `FixedLineView` rendering 100 numbered lines with
an active line that advances on a timer. `AutoScrollBehavior.center`
keeps the active line in view as it moves.
