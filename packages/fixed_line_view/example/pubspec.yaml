name: fixed_line_view_example
description: Demo app for fixed_line_view.
publish_to: none

environment:
  sdk: ^3.10.0

dependencies:
  flutter:
    sdk: flutter
  fixed_line_view:
    path: ../

dev_dependencies:
  flutter_lints: ^6.0.0
